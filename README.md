## Install and run the fastlane-sidecar alongside your validator

```bash
curl -sL https://raw.githubusercontent.com/FastLane-Labs/fastlane-sidecar-installer/main/install.sh | bash -s <process-name>
```